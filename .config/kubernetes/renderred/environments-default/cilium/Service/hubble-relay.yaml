apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/managed-by: Helmraiser
    app.kubernetes.io/name: hubble-relay
    app.kubernetes.io/part-of: cilium
    app.tanka.grafana.com: cilium
    helm.sh/chart: cilium-1.20.0
    k8s-app: hubble-relay
  name: hubble-relay
  namespace: cilium
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: grpc
  selector:
    k8s-app: hubble-relay
  type: ClusterIP
